---
import { cva, type VariantProps } from 'class-variance-authority';

const sectionVariants = cva(['font-sans', 'font-bold', 'leading-tight'], {
  variants: {
    size: {
      h1: 'text-5xl',
      h2: 'text-4xl',
      h3: 'text-3xl',
      h4: 'text-2xl',
      h5: 'text-xl uppercase tracking-wider',
      h6: 'text-lg',
    },
  },
});

export type Props = VariantProps<typeof sectionVariants> & {
  as?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p';
};

const { as: As = 'h2', size = 'h2', ...props } = Astro.props;
---

<As class:list={sectionVariants({ size: size })} {...props}>
  <slot />
</As>
